package router

import (
	"github.com/cloudwego/hertz/pkg/app/server"
)

// RegisterManualRoutes registers manually added routes
func RegisterManualRoutes(r *server.Hertz) {
	// All routes are now handled by hertz generator
}
